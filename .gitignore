# macOS resource forks and metadata
._*
.DS_Store
.AppleDouble
.LSOverride

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Environments
.venv
.env.backup
env/
venv/
ENV/
env.bak/
venv.bak/

# --- SECRETS ---
# Ignore environment files containing API keys and other secrets.
.env
.env.pre-qwen-gen

# --- DATA AND KNOWLEDGE BASES ---
# Ignore generated databases and vector stores
data/vectorstore_base/
data/vectorstores_base/
data/vectorstores_user/
data/vectorstores_client/
data/users.db
data/file_tracker.json
temp_uploads/
data/base_docs/
data/images/
data/*.sqlite3
data/*.db

# Source documents to ingest (large PDFs, not for version control)
documents_to_ingest/

# Local model weights
models/*
!models/.gitkeep

# Eval / test outputs (not needed for deployment)
eval_output*.log
eval_results*.json
eval_results*.csv
eval_dataset.json
rag_evaluation_report.csv
results_baseline.json

# Evaluation & fine-tuning scripts (not needed for deployment)
evaluate_rag.py
eval_ragas.py
test_bot_accuracy.py
test_bot_culture.py
generate_training_data.py
generate_embedding_training_data.py
export_pairs_csv.py
finetune_embeddings.py
colab_finetune.ipynb

# Audit / inventory docs (internal use only)
EVAL_DATASET_WITH_SOURCES.md
KNOWLEDGE_BASE_INVENTORY.md
KNOWLEDGE_BASE_QUALITY_AUDIT.md

# Debug / scratch utilities
check_langchain.py
clean_annotations.py

# Local dev tools (machine-specific, not for version control)
cloudflared
.mcp.json

# Ignore large PDFs in the root
Food Group .pdf
Malaysian food portion size photo album copy.pdf
.env.backup
